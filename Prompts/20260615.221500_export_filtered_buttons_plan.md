# User Prompt
Add two more buttons "Export Filtered Data (CSV)" and "Export Filtered URLs (CSV)"

## Proposed Changes

### Extension UI

#### [MODIFY] [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html)
- Add the two new buttons to the footer action group in `logger.html`:
  - `Export Filtered Data (CSV)` with ID `export-filtered-csv-btn` and class `btn btn-primary`.
  - `Export Filtered URLs (CSV)` with ID `export-filtered-urls-csv-btn` and class `btn btn-secondary`.

### Extension Scripting

#### [MODIFY] [logger.js](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.js)
- Attach click event listeners for `export-filtered-csv-btn` and `export-filtered-urls-csv-btn` on DOMContentLoaded.
- Implement the `exportFilteredCSV()` function:
  - Validates if `filteredLogs` contains entries.
  - Generates the CSV string for `filteredLogs` using `generateCSVString()`.
  - Downloads the generated content as a CSV file.
- Implement the `exportFilteredURLsCSV()` function:
  - Validates if `filteredLogs` contains entries.
  - Generates the CSV string with unique URLs for `filteredLogs`.
  - Downloads the generated content as a CSV file.

## Verification Plan

### Manual Verification
- Load the extension in Firefox (or check in browser log console).
- Add log entries by visiting sites/resources.
- Apply a filter (e.g. search term or event type filter).
- Verify the buttons trigger CSV downloads with correct (filtered) entries.
