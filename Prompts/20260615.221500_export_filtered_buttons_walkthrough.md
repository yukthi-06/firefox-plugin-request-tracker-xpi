# User Prompt
Add two more buttons "Export Filtered Data (CSV)" and "Export Filtered URLs (CSV)"

## Changes Made

### Extension UI
- Modified [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html) to add:
  - `Export Filtered Data (CSV)` button.
  - `Export Filtered URLs (CSV)` button.

### Extension Logic
- Modified [logger.js](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.js) to:
  - Register click handlers for the two new buttons on DOMContentLoaded.
  - Implement `exportFilteredCSV()` to download filtered logs as CSV.
  - Implement `generateFilteredURLsCSVString()` to map filtered logs into a list of unique URLs formatted as CSV.
  - Implement `exportFilteredURLsCSV()` to download the list of unique filtered URLs as CSV.

## Verification
- Code changes have been successfully written to files.
- The extension can be packaged by running `build.bat` to test the new functionality.
