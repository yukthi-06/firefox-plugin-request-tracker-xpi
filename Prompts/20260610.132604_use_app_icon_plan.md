# User Prompt
use app-icon.png as plugin icon.
Copy it to extension/icons as necessary

# Implementation Plan - Use app-icon.png as Plugin Icon

## Proposed Changes

### Extension Metadata

#### [MODIFY] [manifest.json](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/manifest.json)
- Change `"default_icon"` inside `"action"` from `"icons/icon.svg"` to `"icons/app-icon.png"`.
- Add `"icons"` entry in the root of the manifest pointing to `icons/app-icon.png` for sizes `48`, `96`, and `128`.

### Build Automation

#### [MODIFY] [build.bat](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/build.bat)
- Add a copy command to automatically copy the `app-icon.png` from the root directory to `extension/icons/app-icon.png` prior to packaging the extension.

## Verification Plan

### Manual Verification
- Run `build.bat` on the command line to copy the file and package the extension.
- Inspect the output XPI or test the loaded extension to verify the new icon is displayed.
