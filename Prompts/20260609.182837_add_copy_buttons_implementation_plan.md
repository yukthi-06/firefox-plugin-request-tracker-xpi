# Add new copy-to-clipboard buttons

User Prompt:
```
Add new button with label "Copy All Data To Clipboard (CSV)" as third button 
and
Add new button with label "Copy All URLs To Clipboard (TXT)" as fourth button 
and
Add new button with label "Copy Filtered Data To Clipboard (CSV)" as fifth button 
and
Add new button with label "Copy Filtered URLs To Clipboard (TXT)" as sixth button 
```

## User Review Required

> [!NOTE]
> We will add the buttons in the footer right container. We'll use the browser's Clipboard API (`navigator.clipboard.writeText`) to copy the generated CSV/TXT formats directly to the user's clipboard.

## Proposed Changes

### UI & Layout

#### [MODIFY] [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html)
- Add 4 new buttons inside the `<div class="footer-right">` container:
  1. `Copy All Data To Clipboard (CSV)` with id `copy-all-csv-btn`
  2. `Copy All URLs To Clipboard (TXT)` with id `copy-all-txt-btn`
  3. `Copy Filtered Data To Clipboard (CSV)` with id `copy-filtered-csv-btn`
  4. `Copy Filtered URLs To Clipboard (TXT)` with id `copy-filtered-txt-btn`

#### [MODIFY] [logger.css](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.css)
- Style `.footer-right` as a flex container with wrapping to handle multiple buttons gracefully:
  ```css
  .footer-right {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
  }
  ```

### Functionality

#### [MODIFY] [logger.js](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.js)
- Attach click event listeners for the four new buttons.
- Create functions:
  - `copyAllCSV()`: Generates the full CSV text of all logs (reusing logic from `exportCSV()`), copies it to the clipboard, and shows a temporary alert or visual feedback (e.g. alert or toast/temporary text, or simple alert). We can use a simple helper to copy to clipboard and alert "Copied successfully!".
  - `copyAllTXT()`: Generates the list of URLs of all logs, copies it to the clipboard.
  - `copyFilteredCSV()`: Generates the CSV of only `filteredLogs`, copies it to the clipboard.
  - `copyFilteredTXT()`: Generates the list of URLs of only `filteredLogs`, copies it to the clipboard.

## Verification Plan

### Manual Verification
- Test all 4 buttons to verify they copy the expected data to the clipboard.
- Verify that filtered copy functions copy only the filtered items matching current search terms or drop-down filters.
