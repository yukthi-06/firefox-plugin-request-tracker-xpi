# User Prompt
replace "Export" with the word "Save"
Make the 8 buttons in two rows of 4 each.
All Save buttons on top
All Copy buttons on bottom

## Changes Made

### Extension UI
- Modified [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html) to:
  - Replace "Export" with "Save" in button label texts.
  - Wrap buttons in two `.button-row` elements (Top row: 4 Save buttons, Bottom row: 4 Copy buttons).
- Modified [logger.css](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.css) to:
  - Lay out the buttons into two rows using Flexbox on `.button-row` and `flex-direction: column` on the outer `.footer-right` container.

## Verification
- Verified code changes are successfully written.
- Buttons are organized in 2 horizontal rows with the requested labels.
