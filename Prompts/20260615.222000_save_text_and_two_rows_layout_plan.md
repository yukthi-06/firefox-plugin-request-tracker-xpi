# User Prompt
replace "Export" with the word "Save"
Make the 8 buttons in two rows of 4 each.
All Save buttons on top
All Copy buttons on bottom

## Proposed Changes

### Extension UI

#### [MODIFY] [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html)
- Change button text from "Export" to "Save".
- Wrap the buttons into two rows (using `button-row` divs) containing 4 buttons each:
  - Top row: 4 Save buttons.
  - Bottom row: 4 Copy buttons.

#### [MODIFY] [logger.css](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.css)
- Add styling to stack `.button-row` containers vertically.
- Use Flexbox on `.button-row` to keep the 4 buttons horizontal.

## Verification Plan
- Verify that buttons are correctly formatted into two rows.
- Verify button text has changed to "Save" instead of "Export".
