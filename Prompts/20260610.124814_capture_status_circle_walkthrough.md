# User Prompt
on the left side of the "Start Capture" or "Stop Capture" button, have a circle which can show GREEN color for Capturing active and RED color for Capturing Stopped status

## Changes Implemented

### HTML / UI Layout
- Added the status indicator circle element (`#capture-status-indicator`) inside the `.header-actions` section of [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html):
```html
      <div class="header-actions">
        <div id="capture-status-indicator" class="status-circle active"></div>
        <button id="toggle-capture-btn" class="btn btn-secondary" style="margin-right: 8px;">Stop Capture</button>
        <button id="clear-btn" class="btn btn-danger">Clear Session</button>
      </div>
```

### CSS Styling
- Added visual styles in [logger.css](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.css) for `.status-circle` and `.header-actions`:
  - Centered child components using Flexbox inside `.header-actions`.
  - Added transitions for `background-color`, `border-color`, and `box-shadow`.
  - Defined `.active` class showing a glowing green color and `.stopped` class showing a glowing red color.

### JavaScript Logic
- Updated `updateTrackingStateUI()` in [logger.js](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.js) to dynamically update the class of the `#capture-status-indicator` element depending on the tracking status (`trackingActive` true/false).

---

## Verification Performed
- Task list created and tracked at [20260610.124814_capture_status_circle_task.md](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/Prompts/20260610.124814_capture_status_circle_task.md).
- Updated code logic for dynamic UI synchronization.
