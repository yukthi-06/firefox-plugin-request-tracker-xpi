# User Prompt
```
for the final output XPI, suffix timestamp to filename in this format yyyyMMdd.hhmmss
```

# Walkthrough: Timestamped XPI Filename

We have updated the build system in [build.bat](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/build.bat) to suffix the current date and time to the generated XPI package name.

## Changes Made
- Updated [build.bat](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/build.bat) to run a background PowerShell-based date formatter command within CMD to fetch the timestamp `yyyyMMdd.HHmmss` reliably.
- Suffixes this timestamp to the output filename: `session-resource-logger_yyyyMMdd.hhmmss.xpi`.

## Packaging
1. Navigate to the project root in CMD.
2. Run:
   ```cmd
   build.bat
   ```
3. A unique file (e.g. `session-resource-logger_20260609.175451.xpi`) will be generated inside the workspace root.
