# User Prompt
Save filtered URLs is saving as CSV. It should be TXT.
Change the label also

## Changes Made

### Extension UI
- Modified [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html) to change button ID `export-filtered-urls-csv-btn` to `export-filtered-urls-txt-btn` and update label to `💾 Save Filtered URLs (TXT)`.

### Extension Scripting
- Modified [logger.js](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.js) to:
  - Register click listener for `export-filtered-urls-txt-btn`.
  - Replace `exportFilteredURLsCSV()` and helper `generateFilteredURLsCSVString()` with `exportFilteredURLsTXT()`.
  - Use `generateTXTString()` to format filtered URLs as plain text (.txt) file download.

## Verification
- Verified button downloads plain text (.txt) file containing newlines-separated unique URLs matching the active filters.
