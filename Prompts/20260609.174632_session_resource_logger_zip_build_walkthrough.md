# User Prompt
```
user ZIP.EXE instead of TAR for the build.bat
```

# Walkthrough: Updated Build Script using zip.exe

We have updated the build system to use `zip.exe` instead of `tar` for compiling the Firefox extension package.

## Changes Made
- Modified [build.bat](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/build.bat) to change directory into `extension` and run `zip -r ..\session-resource-logger.xpi *` to compress the extension files recursively.
- Restored current directory to workspace root immediately after compression.

## Build Instructions
1. Open a command prompt (`cmd`) and navigate to the project workspace root.
2. Ensure `zip.exe` is available in your system `PATH` (or install a CLI zip tool if not already present).
3. Run the build script:
   ```cmd
   build.bat
   ```
4. A new packaged `session-resource-logger.xpi` file will be created in the workspace root.
