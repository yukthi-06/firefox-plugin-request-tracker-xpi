# User Prompt
Save filtered URLs is saving as CSV. It should be TXT.
Change the label also

## Proposed Changes

### Extension UI

#### [MODIFY] [logger.html](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.html)
- Change button text of filtered URL saving to `💾 Save Filtered URLs (TXT)`.
- Change button ID to `export-filtered-urls-txt-btn`.

### Extension Scripting

#### [MODIFY] [logger.js](file:///x:/Projects_X/0_Active/A_XPI_Firefox_Plugin/firefox-plugin-request-tracker-xpi_GITHUB/extension/logger.js)
- Update click listener for `export-filtered-urls-txt-btn`.
- Implement `exportFilteredURLsTXT()` function to use `generateTXTString(filteredLogs)` and download it as a plain text file `.txt`.

## Verification Plan
- Verify that clicking "Save Filtered URLs (TXT)" downloads a `.txt` file containing the unique list of URLs matching the current filters.
